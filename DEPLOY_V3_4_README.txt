ICS ISSUE CANVAS v3.4

Deploy the contents of this ZIP to the same Netlify site/environment as v3.3.
Required existing environment variables remain unchanged, including AIRTABLE and OPENAI credentials used by the current build.

Key new serverless function:
- /.netlify/functions/generate-canvas-component

After deployment, open an existing issue. It should land directly on Issue Canvas.
Proof order remains locked: Norfolk Spotlight -> PBS -> one niche issue -> only then Master Content Ideas and Archive Recovery.
