# Read This First — ICS Phase 1A

This is the corrected Phase 1A repository package.

The previous ZIP contained an obsolete `UPLOAD_INSTRUCTIONS.txt`. That file has been replaced.

## Current action

Keep this extracted folder ready. Do not upload it until the guided GitHub step.

## What Phase 1A proves

- Netlify can securely read Airtable Publications and Issues.
- The browser never receives the Airtable token.
- The existing ICS builder and reference pages remain intact.
- `/app/` becomes the new connected application shell.

## Safety

This release is read-only and cannot change Airtable records.
