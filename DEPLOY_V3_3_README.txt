ICS Issue Canvas v3.3 — Corrected Deploy Package

This package synchronizes the v3.3 Issue Canvas UI across:
- /index.html
- /app/index.html
- /instantcustomersystem-main/index.html

Upload/extract the complete package into the GitHub repository and replace existing files.
After Netlify finishes deploying, hard-refresh the live ICS page (Ctrl+F5).

Expected version: Issue Canvas v3.3
Expected screen: drag-and-drop Issue Canvas, not the v3.2 checklist planner.
