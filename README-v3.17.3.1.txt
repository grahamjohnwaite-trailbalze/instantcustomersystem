ICS v3.17.3.1 — Step 8 QA State Fix

Scoped Step 8 UI/state stabilisation.
- Fix QA Exceptions is disabled until a fresh v3.17.3.1 QA report exists with >0 hard fixes.
- Successful QA must visibly populate the selectable report.
- QA failure is shown as selectable on-screen status instead of leaving a blank report.
- Old QA state from earlier builds is treated as stale.
- No changes to Steps 1–7, Masters, support counts, or assembly logic.
